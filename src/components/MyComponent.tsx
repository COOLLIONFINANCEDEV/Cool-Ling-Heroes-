import React from 'react';
import Button from '@mui/material/Button';

function MyComponent() {
  return (
    <div>
      <Button variant="contained" color="primary">
        Mon bouton Material-UI
      </Button>
    </div>
  );
}

export default MyComponent;
